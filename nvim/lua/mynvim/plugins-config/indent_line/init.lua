require('ibl').setup {
    indent = {
        char = "▎",
    },
    exclude = {
        filetypes = {"dashboard"},
        buftypes = {"terminal", "NvimTree", "nofile", "quickfix", "propmpt"}
    }
}
